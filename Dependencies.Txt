streamlit==1.26.0
pyngrok==5.2.2
scikit-learn==1.3.0  # If you're using a model trained with scikit-learn
pandas==2.1.1        # If you're working with datasets
numpy==1.23.5        # For numerical operations


To install the dependencies, you can either:

1. Create a requirements.txt file with the contents above and run:
pip install -r requirements.txt
--------------------------------
2.Alternatively, install them individually via:
pip install streamlit pyngrok scikit-learn pandas numpy
